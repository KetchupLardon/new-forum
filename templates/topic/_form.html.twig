{{ form_start(form) }}
    {{ form_widget(form) }}
    <div style="text-align:center;margin-top:20%;margin-bottom:10%;">
    <button class="btn btn-success" style="width:200px;">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
